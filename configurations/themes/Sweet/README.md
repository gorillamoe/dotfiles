This is a hard-copy from the
[upstream repository](https://github.com/EliverLara/Sweet)

With some minor modifications to the GTK-4 CSS file,
to make it work in my specific setup with symlinks.

```sh
gsettings set org.gnome.desktop.interface gtk-theme "Sweet"
gsettings set org.gnome.desktop.wm.preferences theme "Sweet"
```

